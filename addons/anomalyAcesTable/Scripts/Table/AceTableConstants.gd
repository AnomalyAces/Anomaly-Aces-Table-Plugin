@tool
class_name AceTableConstants extends Node

enum ColumnType {
	NONE,
	LABEL,
	BUTTON, 
	TEXTURE_RECT,
	SELECTION
}

enum ColumnSort {
	NONE,
	SORT_ASCENDING,
	SORT_DESCENDING
}

enum Align {
	LEFT,
	CENTER,
	RIGHT,
	FILL
}

enum ImageAlign {
	LEFT,
	RIGHT,
}

enum ButtonType {
	ICON,
	TEXT,
	CHECKBOX,
	COMBO,
	HEADER
}

enum TextType {
	ICON,
	TEXT,
	COMBO,
	LINK,
	HEADER
}

